<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- You can make custom themes with custom style items for activities and views here. -->

    <style name="AppTheme" parent="android:Theme.Material">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:colorBackground">@color/bootstrap_background</item>
        <item name="android:navigationBarColor">@color/bootstrap_background</item>
    </style>

    <style name="GamepadButton" />
</resources>
